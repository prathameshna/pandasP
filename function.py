# We can provide a default value while creating a function. This way the function assumes a defualt value enven if a value is not provided in the function call for that argument.
 
def average(a=2,b=2):
     print("The Addtion is",a+b)
    
average(2,4)